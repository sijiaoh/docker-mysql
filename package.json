{
  "name": "docker-mysql",
  "packageManager": "yarn@3.0.2",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "build": "tsc --project tsconfig.build.json",
    "fix": "gts fix",
    "prepublish": "yarn run build"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "sijiaoh-gts": "^0.5.4",
    "typescript": "^4.0.3"
  }
}

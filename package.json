{
  "name": "docker-mysql",
  "packageManager": "yarn@3.0.2",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "build": "tsc --project tsconfig.build.json",
    "fix": "gts fix",
    "prepublish": "yarn run build"
  },
  "dependencies": {
    "commander": "^8.3.0",
    "execa": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.4",
    "sijiaoh-gts": "^0.5.4",
    "typescript": "^4.4.4"
  }
}
